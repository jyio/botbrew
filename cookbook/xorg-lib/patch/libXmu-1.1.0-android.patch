--- src/LocBitmap.c
+++ src/LocBitmap.c
@@ -97,8 +97,12 @@
 {
 
 #ifndef BITMAPDIR
+#ifdef ANDROID
+#define BITMAPDIR "/system/include/X11/bitmaps"
+#else
 #define BITMAPDIR "/usr/include/X11/bitmaps"
 #endif
+#endif
 
     Display *dpy = DisplayOfScreen (screen);
     Window root = RootWindowOfScreen (screen);
