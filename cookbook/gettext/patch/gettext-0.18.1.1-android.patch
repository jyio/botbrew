--- gettext-tools/src/msginit.c
+++ gettext-tools/src/msginit.c
@@ -1073,7 +1073,7 @@
   struct passwd *pwd;
 
   pwd = get_user_pwd ();
-#if HAVE_PWD_H
+#if HAVE_PWD_H && !defined ANDROID
   if (pwd != NULL)
     {
       const char *fullname;
